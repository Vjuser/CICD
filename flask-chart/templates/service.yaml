apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-flask-chart
spec:
  type: {{ .Values.service.type }}
  selector:
    app: flask-chart
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5000
      protocol: TCP
